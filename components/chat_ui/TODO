ChatUI:
    - creating the wireframe @done
    - creating the components @done
    - design the components @done 
    - add useContext for messages @done
    - make the send button functional:
        - append the messages to the message array
        - create a handler function to send the message:
            - combine system message + user message + chat history
            - send the combined message to the backend
            - create a new bot message and append it to the message array
    - make the emogi section responsive

